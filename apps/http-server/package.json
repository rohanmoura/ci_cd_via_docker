{
  "name": "@ci-cd-via-docker/http-server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build ./src/index.ts --outdir ./dist --target bun --sourcemap=external",
    "start": "bun run dist/index.js",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ci-cd-via-docker/database": "workspace:*",
    "@ci-cd-via-docker/shared": "workspace:*",
    "drizzle-orm": "^0.44.5",
    "hono": "^4.13.7",
    "zod": "^4.5.4"
  }
}
